# name: Packer Build Custom Image

# on:
#   push:
#     branches:
#       - main

# jobs:
#   integration-test:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3

#       - name: Install Node.js dependencies
#         run: npm install

#       - name: Run integration tests
#         run: npm test

#   build-artifact:
#     runs-on: ubuntu-latest
#     needs: integration-test
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3

#       - name: Build Node.js app artifact
#         run: |
#           mkdir artifact
#           # Create the artifact by zipping the app files
#           zip -r artifact/webapp.zip .

#       - name: Upload artifact
#         uses: actions/upload-artifact@v3
#         with:
#           name: nodejs-app-artifact
#           path: artifact/webapp.zip

#   packer-build:
#     runs-on: ubuntu-latest
#     needs: build-artifact
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3

#       - name: Download Node.js app artifact
#         uses: actions/download-artifact@v3
#         with:
#           name: nodejs-app-artifact

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v2
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: us-east-1

#       - name: Install Packer
#         uses: hashicorp/setup-packer@v2

#       - name: Build Packer Image
#         run: |
#           packer build webapp.pkr.hcl
